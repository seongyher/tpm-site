# Minimal Blog Starter

This starter is the smallest maintained site instance for a solo writer. It
keeps support CTAs, citations, announcements, and advanced curation out of the
default path while preserving articles, authors, categories, RSS, search, and a
simple homepage.

Run it with:

```sh
SITE_INSTANCE_ROOT=examples/starters/minimal-blog just build
```
