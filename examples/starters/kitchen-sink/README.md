# Kitchen Sink Starter

This starter is a compact feature demo. It keeps content small while exercising
announcements, support links, collections, citations, bibliography, PDFs, search,
feeds, authors, categories, and tags.

Run it with:

```sh
SITE_INSTANCE_ROOT=examples/starters/kitchen-sink bun run build
```
