---
import { ChevronLeft, ChevronRight } from "lucide-astro";

import IconButton from "../../ui/IconButton.astro";

interface Props {
  itemCount: number;
}

const { itemCount } = Astro.props;
---

<div
  class="min-w-0 items-center justify-center gap-3"
  data-home-featured-controls
  hidden
>
  <IconButton
    label="Previous featured item"
    size="sm"
    variant="ghost"
    data-home-featured-previous
  >
    <ChevronLeft class="size-4" aria-hidden="true" />
  </IconButton>
  <div class="min-w-0 items-center gap-2" data-home-featured-indicators hidden>
    {
      Array.from({ length: itemCount }, (_, index) => (
        <button
          type="button"
          class="text-muted-foreground focus-visible:outline-ring [&[aria-current=true]>span]:bg-primary inline-flex size-6 items-center justify-center rounded-full border-0 bg-transparent p-0 transition-colors focus-visible:outline-2 focus-visible:outline-offset-2"
          aria-label={`Show featured item ${index + 1}`}
          aria-current={index === 0 ? "true" : undefined}
          data-home-featured-indicator
          data-home-featured-index={String(index)}
        >
          <span class="bg-muted block size-2 rounded-full transition-colors" />
        </button>
      ))
    }
  </div>
  <IconButton
    label="Next featured item"
    size="sm"
    variant="ghost"
    data-home-featured-next
  >
    <ChevronRight class="size-4" aria-hidden="true" />
  </IconButton>
</div>
