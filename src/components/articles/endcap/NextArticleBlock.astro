---
import ArticleList from "../lists/ArticleList.astro";
import SectionHeader from "../../ui/SectionHeader.astro";
import type { ArticleListItem } from "../../../lib/content/article-list";
import { articlesArchiveUrl } from "../../../lib/routes/routes";

interface Props {
  headingId?: string;
  item?: ArticleListItem | undefined;
  label?: "Next Article" | "Previous Article";
}

const {
  headingId = "next-article-heading",
  item,
  label = "Next Article",
} = Astro.props;
---

{
  item !== undefined && (
    <section
      class="grid min-w-0 gap-4"
      aria-labelledby={headingId}
      data-article-continuity
    >
      <SectionHeader
        headingId={headingId}
        title={label}
        actionHref={articlesArchiveUrl()}
        actionLabel="View more"
      />
      <ArticleList items={[item]} />
    </section>
  )
}
