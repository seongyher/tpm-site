---
import SectionNavItem from "./SectionNavItem.astro";
import type { SectionNavItem as SectionNavItemData } from "../../lib/site/navigation";

interface Props {
  item: SectionNavItemData;
}

const { item } = Astro.props;
---

<SectionNavItem item={item} />
