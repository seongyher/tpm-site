# Expected Diagnostics

The default fixture is structurally valid for the initial Rust workspace
discovery tests and should not emit missing-path diagnostics.

Future tests may add deliberately invalid fixture variants next to this one.
Those variants should document their expected diagnostic codes here or in a
nearby fixture-specific note.
